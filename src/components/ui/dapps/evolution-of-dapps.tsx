import { Spacer } from '@nextui-org/react';
import { Section } from '@primitives';

const EvolutionOfDapps: React.FC = () => {
  return (
    <Section>
      <Spacer y={5} />
    </Section>
  );
};

export default EvolutionOfDapps;
