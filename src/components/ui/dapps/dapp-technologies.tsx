import { Spacer } from '@nextui-org/react';
import { Section } from '@primitives';

const DappTechnologies: React.FC = () => {
  return (
    <Section>
      <Spacer y={5} />
    </Section>
  );
};

export default DappTechnologies;
